# ── Core pipeline ─────────────────────────────────────────────────────────────
pydantic>=2.7
groq>=0.9
instructor>=1.3.0
supabase>=2.4
python-dotenv>=1.0
pdfplumber>=0.11
requests>=2.31

# ── Sarvam-M (Indic-native LLM for Bengali content)
# Uses OpenAI-compatible client — openai is a transitive dep of instructor
openai>=1.0

# ── Observability (optional — install to enable Phoenix tracing)
# pip install arize-phoenix-otel \
#             openinference-instrumentation-groq \
#             openinference-instrumentation-openai
# Then set PHOENIX_ENDPOINT env var to activate
# (openai instrumentor also covers Sarvam-M, which uses the OpenAI client)
# arize-phoenix-otel>=0.7
# openinference-instrumentation-groq>=0.1
# openinference-instrumentation-openai>=0.1

# ── Phase 6 — Marker + Surya OCR for Bengali textbook PDFs ────────────────────
# NOT installed by default — these pull torch (~2GB) + Surya weights.
# Run OCR only via .github/workflows/ocr_textbook.yml (separate workflow,
# 60-min timeout). Regular ingest runs never touch Marker.
# To install on the OCR worker:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install marker-pdf
# marker-pdf>=1.0      # wraps Surya internally

# Phase 9 (Ganak v2 — pgvector semantic coverage):
# pgvector>=0.3

# Phase 9 (DSPy MIPROv2 prompt optimiser):
# dspy-ai>=2.4
